# GymLog 健身記錄

離線優先的健身重量記錄 PWA。深色介面、自製數字盤、好友房間即時連線（Firestore）。

- 用 iPhone/Android 瀏覽器開啟 GitHub Pages 網址 → 加入主畫面 → 離線可用
- 訓練紀錄存在裝置 localStorage，不會上傳到本 repo
- `firebase-config.js` 的 web config 為公開設計，資料安全由 Firestore 規則控管
